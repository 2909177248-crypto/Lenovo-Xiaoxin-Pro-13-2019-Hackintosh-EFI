# 联想小新 Pro 13 2019 黑苹果全流程使用与避坑手册

本文档为 **联想小新 Pro 13 2019 (Intel 10代)** 深度定制的详尽使用指南。无论你是第一次安装黑苹果的新手，还是刚更换了博通免驱网卡的老玩家，请在操作前仔细通读。

---

## 目录
1. [BIOS 推荐设置](#一-bios-推荐设置)
2. [双版本 EFI 选用与部署](#二-双版本-efi-选用与部署)
3. [SMBIOS 三码注入与 Apple ID 登录](#三-smbios-三码注入与-apple-id-登录)
4. [博通 BCM94360 换卡与天线安装避坑 (核心重点!)](#四-博通-bcm94360-换卡与天线安装避坑)
5. [macOS 14 Sonoma 博通网卡 OCLP 激活指南](#五-macos-14-sonoma-博通网卡-oclp-激活指南)
6. [蓝牙耳机 (如 AirPods Pro) 音质断续排查与优化](#六-蓝牙耳机-airpods-pro-音质断续排查与优化)
7. [屏幕分辨率与 BetterDisplay 调优](#七-屏幕分辨率与-betterdisplay-调优)
8. [常见问题排障 FAQ](#八-常见问题排障-faq)

---

## 一. BIOS 推荐设置

开机在出现 Lenovo 标志时连续按 **`F2`**（或 `Fn+F2`）进入 BIOS 设置：

* **Configuration (配置)**:
  - `SATA Controller Mode`: **AHCI** *(严禁设为 Intel RST/RAID)*
* **Security (安全)**:
  - `Secure Boot (安全启动)`: **Disabled (关闭)** *(黑苹果必须关闭)*
  - `Intel Platform Trust Technology (PTT)`: 可保持 Enabled
* **Boot (启动)**:
  - `Boot Mode`: **UEFI**
  - `Fast Boot (快速启动)`: **Disabled (关闭)**
* **Exit**:
  - 按 **`F10`** 保存并退出重启。

---

## 二. 双版本 EFI 选用与部署

本项目在仓库中提供了两个独立的 EFI 文件夹：

### 1. 怎么选？
* **未拆机原装网卡** ➡️ 使用 **`EFI-Intel-Default/EFI`**
  - 适合原装 Intel AX201 (CNVi) / AC9560 / AC9462 用户；
  - 默认加载 `AirportItlwm.kext` 与 Intel 蓝牙驱动，开机 Wi-Fi / 蓝牙即可用。
* **已更换白苹果网卡** ➡️ 使用 **`EFI-Broadcom-BCM94360/EFI`**
  - 适合换装了 **BCM94360Z4 / BCM94360CS2 / BCM94360NG** 的用户；
  - 已彻底关闭网卡 ASPM 节能（解决测速死机），解锁原生隔空投送、随航、接力。

### 2. 部署方法
1. 在 macOS 打开终端，挂载硬盘内置的 EFI 分区：
   ```bash
   sudo diskutil mount /dev/disk0s1
   ```
2. 打开访达中挂载的 `EFI` 盘（路径 `/Volumes/EFI`）；
3. 将你选择的文件夹内的 **`EFI`** 目录（包含 `BOOT` 和 `OC` 两个子文件夹）完整拷贝进去；
4. **务必执行一次 NVRAM 重置**：重启电脑，在开机引导界面按 **空格键** 选中 **`Reset NVRAM`** 回车重启。

---

## 三. SMBIOS 三码注入与 Apple ID 登录

为了能正常使用 App Store、iCloud、iMessage，必须填入属于你个人的唯一三码：

1. 下载 [GenSMBIOS 工具](https://github.com/corpnewt/GenSMBIOS)；
2. 运行 `GenSMBIOS.command`，输入 `1` 下载 MacSerial 库；
3. 输入 `3`，输入机型代号 **`MacBookPro16,3`** 回车，工具会生成一组三码：
   - `Type`: MacBookPro16,3
   - `Serial`: (这就是 SystemSerialNumber)
   - `Board Serial`: (这就是 MLB)
   - `SmUUID`: (这就是 SystemUUID)
4. 使用 ProperTree 或文本编辑器打开你的 `EFI/OC/config.plist`；
5. 定位到 `PlatformInfo` -> `Generic`，将上面三项填入对应字段保存。

---

## 四. 博通 BCM94360 换卡与天线安装避坑

这是无数小新 Pro 13 机主换卡后翻车最严重的地方，请务必注意：

### 1. 为什么蓝牙耳机声音断断续续？（半米就断）
* 小新 Pro 13 原装只有 **2 根天线**（一黑一白）。
* 你的 BCM94360Z4 / CS2 卡上有 **3~4 个天线座**，其中包含专门的 **`BT`** 蓝牙天线座！
* **致命失误**：如果把原机的 2 根线全部插在 WiFi 接口上，导致 **BT 接口空置**，金属机身会把蓝牙信号完全屏蔽，蓝牙距离只有 30cm！
* **解决方案**：
  1. 必须保证有 1 根天线插在写有 **`BT`** 的座子上；
  2. 推荐淘宝花 3 块钱买一条 **`IPEX4 贴片天线`** 接上，做成 3 天线满血。

### 2. 贴片天线严禁直接贴在金属壳内侧！
* 小新 Pro 13 整个底盖是**纯铝合金金属**；
* 如果贴片天线直接贴在金属后盖或电池屏蔽层上，**电磁波会被金属板直接吸收/反向抵消（短路）**！
* **正确贴法**：必须贴在**塑料件**上（如黑色塑料扬声器音腔外壳、或转轴附近的塑料导光条旁）。

---

## 五. macOS 14 Sonoma 博通网卡 OCLP 激活指南

macOS 14 (Sonoma) 剥离了旧款博通驱动，更换 BCM94360 后必须执行以下四步：

1. **确认使用本项目的 `EFI-Broadcom-BCM94360`**：
   - 本配置已默认开启 `IOSkywalkFamily` 降级屏蔽、`amfi=0x80` 与 `AMFIPass`。
2. **连接网络进入桌面**：
   - 首次开机博通 Wi-Fi 暂未激活，请通过手机 USB 共享网络或 Type-C 有线网卡让电脑联网。
3. **运行 OpenCore-Patcher (OCLP)**：
   - 打开 [OpenCore-Patcher.app](https://github.com/dortania/OpenCore-Legacy-Patcher)；
   - 点击 **`Post-Install Root Patch`**；
   - 会检测到 `Networking: Modern Wireless` 补丁，点击 **`Start Root Patching`** 并输入密码；
4. **重启电脑**：
   - 补丁写入完成重启后，原生 Wi-Fi、隔空投送 (AirDrop) 瞬间全部满血复活！

---

## 六. 蓝牙耳机 (AirPods Pro) 音质断续排查与优化

如果连接 AirPods Pro 发现声音卡顿，绝大多数是因为系统默认把麦克风占满了：

1. **释放蓝牙麦克风带宽 (立竿见影)**：
   - 打开 **系统设置 -> 声音 -> 输入**；
   - **严禁选中 AirPods Pro**！请手动点选 **`内建麦克风 (Built-in Internal Mic)`**。
   - *(原因：一旦麦克风连着耳机，蓝牙会强制切入双向通话窄带协议，音乐带宽被榨干导致断续)*。
2. **关闭多设备反复争抢**：
   - 打开 **系统设置 -> 蓝牙 -> AirPods Pro 旁边的 ⓘ**；
   - 将 **“连接到此 Mac”** 从“自动”改为 **“上次连接到此 Mac 时”**。

---

## 七. 屏幕分辨率与 BetterDisplay 调优

小新 Pro 13 拥有 2560x1600 (16:10) 的 2.5K 超清屏，推荐调优策略：

1. **最佳视觉档位**：
   - 🥇 **`1440 x 900 (HiDPI)`**：苹果官方 13.3 寸 MacBook Pro 默认黄金比例，窗口大、字体最适中；
   - 🥈 **`1280 x 800 (HiDPI 2x)`**：物理点对点渲染，字迹最黑最锐利，零插值损耗。
2. **防止合盖开盖后分辨率自动跳变**：
   - 打开后台常驻的 **BetterDisplay 设置 (Settings) -> Displays (显示器)**；
   - 勾选 **`Auto-reapply resolution on connect / wake`**（唤醒时自动恢复此分辨率）；
   - 勾选 **`Set as default boot resolution`**。

---

## 八. 常见问题排障 FAQ

#### Q1：开机进入桌面触控板突然动不了？
* **答**：99% 是不小心误按了联想专属的触控板开关！直接按一下键盘第一排的 **`F6`** 键（或 `Fn + F6`）即可立刻解锁恢复。

#### Q2：以前博通网卡测速或看视频突然直接死机、绿线？
* **答**：这是原机配置开启了 ASPM 导致的 PCIe 挂死。本项目发布的 `EFI-Broadcom-BCM94360` 已经将 `Pci(0x1C,0x0)` 桥的 `pci-aspm-default` 改为了 `0`，彻底根治此问题，请放心测速。

#### Q3：屏幕上方在鼠标滑动或网页滚动时偶尔闪一下？
* **答**：这是核显 RC6 休眠唤醒的时钟毛刺。本项目已在 `boot-args` 中注入了 `forceRenderStandby=0`，闪烁已被彻底消除。

---
祝你在联想小新 Pro 13 上享受极其丝滑的 macOS 旅程！
